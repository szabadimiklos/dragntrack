import { createTheme } from '@mui/material'

const themeDragAndTrack = createTheme({
	// Define theme/style overrides here
	palette: {
		type: 'light',
		primary: {
			main: '#4d9e00',
		},
		secondary: {
			main: '#e05b27',
		},
	},
});

export default themeDragAndTrack